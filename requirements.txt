# =============================================================================
# SignalScope — Python dependencies
# =============================================================================
# Python 3.10+ recommended.
#
# NOTE ON PyTorch: the pins below resolve to the default PyPI build (CUDA-enabled
# wheel on Linux, CPU/CUDA on other platforms). If you want a smaller CPU-only
# install, run instead:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# and then `pip install -r requirements.txt`.
#
# Deep-learning
torch==2.14.0
torchvision==0.29.0

# Data / numeric
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
scikit-learn==1.9.1

# Imaging
Pillow==12.3.0
opencv-python-headless==5.0.0.93
scikit-image==0.26.0

# Plotting / reports
matplotlib==3.11.2

# Config
PyYAML==6.0.3

# Web API
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32

# Provenance (EXIF)
piexif==1.1.3

# Utilities
requests==2.34.2
tqdm==4.70.1

# Testing
pytest==9.1.1
httpx==0.28.1
